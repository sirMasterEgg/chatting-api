// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "windows"]
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// model User {
//   id          String   @id @default(auto()) @map("_id") @db.ObjectId
//   email       String   @unique
//   username    String   @unique
//   password    String
//   name        String?
//   friends     User[]   @relation("UserFriends", fields: [friendIDs], references: [id])
//   friendIDs   String[] @db.ObjectId
//   friendOfs   User[]   @relation("UserFriends", fields: [friendOfIDs], references: [id])
//   friendOfIDs String[] @db.ObjectId
// }
model User {
  id        String       @id @default(auto()) @map("_id") @db.ObjectId
  email     String       @unique
  username  String       @unique
  password  String
  name      String?
  friends   Friendship[] @relation("friend")
  friendOfs Friendship[] @relation("friendOf")
}

model Friendship {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  user     User   @relation("friend", fields: [userId], references: [id])
  userId   String @db.ObjectId
  friend   User   @relation("friendOf", fields: [friendId], references: [id])
  friendId String @db.ObjectId
  status   Int
}
